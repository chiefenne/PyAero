Customizing PyAero
------------------

`PyAero <index.html>`_ supports customizing its behaviour. The parameters which can be modified are stored in a file called :file:`PSettings.py`.

In order to avoid eye cancer, this time **no** JSON format is used to store configuration parameters.

